=== Task 8: GDAL Raster Read Verification ===

Date: 2026-06-01
Crate: nufrost-gdal

Tests passed:
  - placeholder_gdal_version        — GDAL version string includes dot
  - reader_metadata_synthetic       — shape, raster_size, band_count, nodata match
  - reader_crs_roundtrip            — WKT CRS roundtrip (WGS84)
  - read_band_roundtrip             — 4x8 2-band → read back exact values
  - read_valid_mask                 — Sentinel-2 mask (0 < val < 10000)
  - nodata_none_by_default          — no nodata set → returns None
  - write_evidence_single_band      — writes valid GeoTIFF
  - write_evidence_zhu2015_2band    — writes 2-band Zhu2015 GeoTIFF

Python rasterio verification:
  - Single-band: Shape=(5,7), CRS=EPSG:4326, Nodata=-9999.0, Count=1, dtype=float32
    All values match expected (0..34)
  - Zhu2015 2-band: Shape=(4,6), Count=2, Nodata=-32768.0, dtype=float32
    Band 1 (prediction): 0.0 .. 0.23, Band 2 (QA): 0 .. 3

API surface:
  - RasterReader::open(path) → Result<Self>
  - RasterReader::shape() → (rows, cols)
  - RasterReader::raster_size() → (cols, rows)
  - RasterReader::geo_transform() → Option<GeoTransform>
  - RasterReader::crs_wkt() → Option<String>
  - RasterReader::nodata(band_idx: usize) → Option<f64>
  - RasterReader::read_band(band_idx: usize) → Result<Array2<f64>>
  - RasterReader::read_valid_mask(band_idx) → Result<Array2<bool>>
  - RasterReader::read_valid_mask_custom(band_idx, valid_min, valid_max) → Result<Array2<bool>>
